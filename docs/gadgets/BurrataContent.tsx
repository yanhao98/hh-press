import Content from './BurrataContent.tsx.md';

/**
 * Burrata 的外壳就像 sing-box 建立起的那个安全、加密的隧道，从外面看，它是一个完整、无懈可击的数据包。而它包裹的，正是那 unrestricted（不受限的）、自由流动的互联网数据，也就是最美味的“奶油”部分。
 */
export const BurrataContent = () => {
  return (
    <div style={{}}>
      <button>button</button>
      <Content />
    </div>
  );
};
